# Web Version Instructions

- Use browser-native HTML canvas and JavaScript.
- Use the Python standard library for the local server.
- Send the canvas as PNG data to `/predict`.
- Keep the page usable with keyboard focus and clear status text.
